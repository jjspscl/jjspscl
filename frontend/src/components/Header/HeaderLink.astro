---
interface Props {
    label: string;
    href: string;
}

const { label, href } = Astro.props;
---

<a class="rounded px-2 py-1 font-bold transition-colors duration-200 hover:text-accent" data-astro-prefetch href={href}>
    {label}
</a>
